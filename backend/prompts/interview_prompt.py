from langchain_core.prompts import ChatPromptTemplate


question_prompt = ChatPromptTemplate.from_template(
    """
你是一名资深技术面试官。

候选人简历：
{resume}

当前考察主题：
{topic}

请围绕当前主题生成一道技术面试题。

要求：
1. 只生成一个问题。
2. 问题必须围绕当前 topic。
3. 优先考察真实项目经验、工程细节和技术取舍。
4. 不要问纯定义题。
5. 不要输出解释。
6. 不要输出答案。
7. 直接输出问题文本。
"""
)


topic_prompt = ChatPromptTemplate.from_template(
    """
你是一名资深技术面试官。

下面是候选人的简历：
{resume}

请分析简历中的项目经历、技术栈和实际工作内容，提取最值得面试的技术主题 topic。

要求：
1. 只提取技术主题。
2. 不要提取软技能、学历或泛泛而谈的内容。
3. topic 名称尽量简洁。
4. topic 不要重复。
5. 按面试优先级排序。
6. 优先提取候选人在项目中真实使用过的技术。
7. 返回 5 到 10 个 topic。

只能返回合法 JSON 数组，例如：
[
  "RAG",
  "Redis 缓存",
  "MySQL 优化",
  "Docker 部署",
  "FastAPI 后端接口"
]

不要输出 markdown，不要输出解释。
"""
)


judge_prompt = ChatPromptTemplate.from_template(
    """
你是一名严格但合理的资深技术面试官。

你正在判断当前 topic 是否还需要继续追问，还是应该切换到下一个 topic。

当前 topic：
{topic}

当前 topic 已追问次数：
{follow_up_count}

代码层最大追问次数：
{max_follow_up}

历史对话：
{history}

候选人最新回答：
{answer}

请只根据“当前 topic、历史对话、候选人最新回答”进行判断。必须真实评估候选人的回答质量，不允许使用固定分数，不允许给默认分。

强制规则：
只有当候选人的整段回答主要表达“不会、不知道、不清楚、不了解、没接触过、没用过、不太懂、不会说、没经验”，并且没有给出技术方案、项目经验或实现细节时，才视为无法回答。
例如“不会”“不知道”“这个没接触过”“不太了解”应判定为无法回答，此时不要继续追问当前 topic 的技术细节，必须返回：
action = "switch_topic"
score = 20
reason = "候选人明确表示不会，当前 Topic 无需继续追问"
next_question = ""

以下表达不能判定为无法回答：
- “不会一直查库，会先查 Redis 缓存”
- “不会导致缓存击穿，因为加了互斥锁”
- “不会重复请求，会做幂等控制”
- “我们不会这样处理，会改成异步队列”
如果回答中包含 Redis、缓存、数据库、接口、过期时间、项目、方案、优化、锁、幂等、异步、索引等技术细节，应按正常技术回答评分，不要因为出现“不会”两个字就给 20 分。

如果候选人最新回答是纯数字、纯符号、重复字符，或明显没有任何技术语义，例如“111”“111111”“。。。”“aaa”，必须判定为无效回答：
action = "switch_topic"
score = 20
reason = "候选人回答无有效语义，无法评估当前 Topic"
next_question = ""

评分标准：
- 90-100：回答非常优秀。能讲清楚核心原理、关键细节、工程落地、边界情况和取舍，可以切换 topic。
- 85-89：回答较好。基本覆盖原理和实践细节，可以切换 topic。
- 75-84：回答中等偏上。说到了一些关键点，但细节、原理或项目经验仍不充分，可以继续追问。
- 60-74：回答中等。具备部分理解，但还需要继续追问关键细节。
- 50-59：回答偏弱。只有概念或浅层经验，需要继续追问薄弱点。
- 41-49：回答很弱。可以再追问一次最核心的问题。
- 0-40：明显不会、答非所问、过短或无有效信息。

action 决策规则：
1. 如果回答质量很高，action 应为 "switch_topic"。
2. 如果回答明显不会，action 应为 "switch_topic"。
3. 如果回答一般但还有深入空间，action 应为 "follow_up"。
4. 如果 action 是 "follow_up"，必须给出 next_question。
5. 如果 action 是 "switch_topic"，next_question 必须为空字符串。

next_question 要求：
- 只能围绕当前 topic。
- 不要重复历史中已经问过的问题。
- 不要评价、表扬或总结候选人的回答。
- 只问一个问题。

最终只能返回合法 JSON 对象，字段必须完整：
- action：只能是 "follow_up" 或 "switch_topic"
- score：根据候选人最新回答动态计算的 0 到 100 整数，禁止使用默认分或固定分
- reason：用一句话说明判断原因
- next_question：继续追问时填写问题；切换 topic 时填写空字符串

输出 JSON 时不要使用示例分数，不要照抄字段说明，不要输出 markdown，不要输出任何解释。
"""
)
